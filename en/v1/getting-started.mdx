---
title: Getting Started
---

Welcome to your Rust journey! Before we dive in, here’s a taste of what Rust code can look like when you combine many of its powerful features:

<CodeBlock language="rust">{`
//! This is a documentation comment
// Regular comment

#[derive(Debug, Clone)]
struct Point<T> {
    x: T,
    y: T,
}

impl<T: std::fmt::Display> Point<T> {
    fn display(&self) {
        println!("Point: ({}; {})", self.x, self.y);
    }
}

#[allow(dead_code)]
enum Message<'a> {
    Quit,
    Move { x: i32, y: i32 },
    Write(&'a str),
    ChangeColor(i32, i32, i32),
}

macro_rules! say_hello {
    () => {
        println!("Hello from macro!");
    };
}

fn main() {
    let p = Point { x: 3.14_f32, y: 42.0_f32 };
    p.display();
    
    let msg = Message::Write(r#"Hello, \"Rust\" world!"#);
    match msg {
        Message::Write(text) => println!("Message: {}", text),
        _ => println!("Other message"),
    }
    
    say_hello!();
    
    // Error handling example
    let result: Result<u8, &str> = Err('💥'.to_string().as_str());
    match result {
        Ok(val) => println!("Success: {}", val),
        Err(e) => eprintln!("Error: {}", e),
    }
}
`}</CodeBlock>

This example demonstrates Rust’s syntax, macros, enums, pattern matching, error handling, generics, lifetimes, attributes, doc comments, raw strings, and more. Try editing and highlighting your own code next! 🚀